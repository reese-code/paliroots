{% comment %}
  PALIROOTS Custom Hero Section
  Hero section with image background and text content
{% endcomment %}

<div class="paliroots-hero">
  <!-- Hero Image -->
  {% if section.settings.hero_image != blank %}
    {{ section.settings.hero_image | image_url: width: 2048 | image_tag: class: 'paliroots-hero__image', alt: section.settings.hero_image.alt }}
  {% else %}
    <div class="paliroots-hero__image-placeholder">
      Add your hero image here
    </div>
  {% endif %}

  <!-- Hero Content -->
  <div class="paliroots-hero__content">
    {% if section.settings.hero_title != blank %}
      <h1 class="paliroots-hero__title">{{ section.settings.hero_title }}</h1>
    {% endif %}

    {% if section.settings.hero_description != blank %}
      <p class="paliroots-hero__description">{{ section.settings.hero_description }}</p>
    {% endif %}

    {% if section.settings.hero_button_text != blank and section.settings.hero_button_url != blank %}
      <a href="{{ section.settings.hero_button_url }}" class="paliroots-hero__button">
        {{ section.settings.hero_button_text }}
      </a>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "PALIROOTS Hero",
  "tag": "section",
  "class": "paliroots-hero-section",
  "settings": [
    {
      "type": "header",
      "content": "Hero Image"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Background Image",
      "info": "Recommended size: 2048x1024px or larger"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "hero_title",
      "label": "Hero Title",
      "default": "Shop Winter '24 Collection"
    },
    {
      "type": "textarea",
      "id": "hero_description",
      "label": "Hero Description",
      "default": "The iconic Pali Flag Sweatsuits are back and now available in kids sizes. Alongside them, discover nine new arrivals, blending timeless design with unmatched comfort for every day."
    },
    {
      "type": "text",
      "id": "hero_button_text",
      "label": "Button Text",
      "default": "Shop now"
    },
    {
      "type": "url",
      "id": "hero_button_url",
      "label": "Button Link",
      "default": "/collections/all"
    }
  ],
  "presets": [
    {
      "name": "PALIROOTS Hero",
      "settings": {
        "hero_title": "Shop Winter '24 Collection",
        "hero_description": "The iconic Pali Flag Sweatsuits are back and now available in kids sizes. Alongside them, discover nine new arrivals, blending timeless design with unmatched comfort for every day.",
        "hero_button_text": "Shop now",
        "hero_button_url": "/collections/all"
      }
    }
  ]
}
{% endschema %}